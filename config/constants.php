<?php
    // Project constants for consistent path handling

    // Define project root path (absolute filesystem path)
    define('ROOT', dirname(__DIR__));

    // Define base URL for web paths (relative to web root)
    define('BASE_URL', '/EMS_version_01/EventManagement-TicketOrderingSystem/');
?>